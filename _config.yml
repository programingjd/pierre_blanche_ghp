title: Pierre Blanche
author: Jerome David
favicon: favicon.svg
timezone: Europe/Paris

defaults:
  -
    scope:
      path: ""
      type: posts
    values:
      layout: post
      permalink: /:path/post/:basename
  -
    scope:
      path: ""
      type: pages
    values:
      layout: page
      permalink: /:path/:basename
  -
    scope:
      path: "club.md"
      type: pages
    values:
      layout: club
      permalink: /:path/:basename
  -
    scope:
      path: "falaise.md"
      type: pages
    values:
      layout: falaise
      permalink: /:path/:basename
